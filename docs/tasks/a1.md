# a1 Worktree Ownership

The `a1` branch owns the common baseline contract and baseline RTL:

- `docs/spec.md`
- `rtl/common/**`
- `rtl/baseline/**`
- baseline-only smoke checks under `tests/a1/**`

It must not modify improved RTL, the shared verification environment, or the
server/PPA flow owned by the other worktrees.

Current deliverable:

1. [x] Parameterized fixed-priority arbiter.
2. [x] One-entry registered AER transmitter.
3. [x] One-entry elastic AER receiver.
4. [x] Legacy request/ack baseline top.
5. [x] Common ready/valid baseline core and `aer_dut` adapter.
6. [x] Independent event-address payload and source sideband propagation.
7. [x] Legacy and comparison-DUT smoke tests.
8. [x] Fixed-priority starvation characterization.
9. [x] Server Xcelium regression and Genus elaboration sanity check.
10. [x] Shared a3 scoreboard regression under server Xcelium.

## Design-server validation

Validated on `snu.polaris.09` on 2026-07-31:

- Xcelium `23.09-s013`
  - legacy request/ack smoke: PASS
  - payload and source-sideband smoke: PASS
  - fixed-priority starvation: PASS (`source0=20`, `source3=0`)
  - a3 shared scoreboard workloads: 4/4 PASS
- Genus `23.14-s090_1`
  - top: `aer_dut`
  - library: GPDK045 `slow_vdd1v0_basicCells.lib`
  - elaboration: PASS
  - unresolved references: none
  - empty modules: none

The shared a3 scoreboard was run against the baseline `aer_dut` with four
sources, 16-bit addresses, and 32 events per source. All workloads completed
with no missing, duplicate, reordered, or corrupted events:

| Workload | Accepted | Emitted | Errors | Avg latency | Max latency | Throughput | Jain fairness | Max wait |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| single | 32 | 32 | 0 | 2.0000 | 2 | 0.492308 | 0.250000 | 1 |
| simultaneous | 128 | 128 | 0 | 2.0000 | 2 | 0.498054 | 1.000000 | 192 |
| burst | 320 | 320 | 0 | 2.0000 | 2 | 0.499220 | 0.833333 | 576 |
| backpressure | 128 | 128 | 0 | 3.5156 | 5 | 0.397516 | 1.000000 | 241 |

Xcelium requires a timescale for every elaborated module. The baseline RTL has
no source-level timing semantics, so server regressions supply the common
simulation setting explicitly:

```sh
xrun -64bit -sv -timescale 1ns/1ps \
  -f rtl/baseline/baseline.f \
  tests/a1/aer_baseline_smoke_tb.sv \
  -top aer_baseline_smoke_tb

xrun -64bit -sv -timescale 1ns/1ps \
  -f rtl/baseline/baseline_dut.f \
  tests/a1/aer_baseline_payload_tb.sv \
  -top aer_baseline_payload_tb

xrun -64bit -sv -timescale 1ns/1ps \
  -f rtl/baseline/baseline_dut.f \
  tests/a1/aer_baseline_starvation_tb.sv \
  -top aer_baseline_starvation_tb
```

Genus elaboration uses an absolute Liberty path because Tcl does not expand
`~` in the `read_libs` argument.

## Exploratory baseline synthesis

The integrated Genus flow completed baseline `syn_generic`, `syn_map`, and
`syn_opt` on 2026-07-31 with these shared comparison settings:

- `NUM_SOURCES=4`, `ADDR_WIDTH=16`
- 5.000 ns clock, 0.250 ns I/O delays, 0.100 ns uncertainty
- GPDK045 `slow_vdd1v0_basicCells.lib`
- library operating condition reported by Genus: `PVT_0P9V_125C`
- vectorless power estimation

Normalized from the native Genus reports:

| Metric | Baseline result |
| --- | ---: |
| Cell area | 432.288 um2 |
| Setup slack | 3.6885 ns |
| Critical delay (`period - slack`) | 1.3115 ns |
| Derived Fmax | 762.485703 MHz |
| Total power | 0.0535469 mW |
| Dynamic power (internal + switching) | 0.0535374 mW |
| Leakage power | 0.000009512 mW |

Power is a vectorless synthesis estimate and is suitable only for the first
baseline/improved comparison. It is not a final activity-annotated result.
The shared result extractor also needs to convert Genus timing from ps to ns
and power from W to mW before its generated `metrics.tsv` can be used.
