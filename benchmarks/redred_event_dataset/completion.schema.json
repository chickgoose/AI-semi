{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/redred-event-import-completion-v2.schema.json",
  "title": "REDRED local captured-byte import completion sentinel v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "status", "receipt_snapshot_sha256", "trace_snapshot_sha256"],
  "properties": {
    "schema": {"const": "redred-event-import-completion-v2"},
    "status": {"enum": ["LOCAL_IMPORT_COMPLETE_UNQUALIFIED", "HOLD_UNSUPPORTED_FORMAT"]},
    "receipt_snapshot_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "trace_snapshot_sha256": {
      "oneOf": [
        {"type": "string", "pattern": "^[0-9a-f]{64}$"},
        {"type": "null"}
      ]
    }
  },
  "allOf": [
    {
      "if": {"properties": {"status": {"const": "LOCAL_IMPORT_COMPLETE_UNQUALIFIED"}}},
      "then": {"properties": {"trace_snapshot_sha256": {"type": "string"}}}
    },
    {
      "if": {"properties": {"status": {"const": "HOLD_UNSUPPORTED_FORMAT"}}},
      "then": {"properties": {"trace_snapshot_sha256": {"type": "null"}}}
    }
  ]
}
